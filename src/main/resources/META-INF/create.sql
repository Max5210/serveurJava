CREATE TABLE athletes(id INT IDENTITY NOT NULL, email VARCHAR(50), password VARCHAR(50), status VARCHAR, PRIMARY KEY (id))
CREATE TABLE coachs(id INT IDENTITY NOT NULL, email VARCHAR(50), password VARCHAR(50), status VARCHAR, PRIMARY KEY (id))
CREATE TABLE trainings(id INT IDENTITY NOT NULL, date DATE, PRIMARY KEY (id))